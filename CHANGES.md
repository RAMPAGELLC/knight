# v1.0.7
```diff
- Removed md5
+ Installed new ``KnightRemotes``, learn more at https://github.com/Metatable-Games/KnightRemotes
+ Misc. Fixes
+ Repo cleanup. Removed useless files.
- Removed the default library and library manifest
+ Updated/fix default config
- Removed sentry example
+ Framework loading optimised for synchronously loading and lazy loading (avg ~5,000ms load down to ~0ms)
- Removed KnightCache and Class:Update()
```